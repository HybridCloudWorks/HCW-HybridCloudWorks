{
  "name": "hcw-availability-probe",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker cron probe for /api/health, reporting AvailabilityData to Application Insights (ADR 0024, T-519)",
  "scripts": {
    "test": "node --test"
  }
}
