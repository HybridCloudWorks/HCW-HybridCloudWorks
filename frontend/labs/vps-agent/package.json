{
  "name": "hcw-labs-vps-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Pull-based lab execution agent for the HybridCloudWorks labs platform. Polls Firestore for queued lab_jobs, runs them in sandboxed Docker containers, writes results back.",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0"
  },
  "overrides": {
    "brace-expansion": "5.0.8",
    "uuid": "11.1.1"
  }
}
