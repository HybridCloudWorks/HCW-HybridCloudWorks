# {{ ansible_managed }}
# Read by the coder-postgres service only. Used at first initialisation of the
# data volume; changing it later needs ALTER USER too (lab-host/README.md,
# "Rotating the PostgreSQL password").
POSTGRES_PASSWORD={{ coder_postgres_password }}
