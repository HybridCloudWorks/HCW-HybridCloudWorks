---
galaxy_info:
  role_name: labs_agent
  namespace: hcw
  author: HybridCloudWorks
  description: The vps-agent job runner as a systemd service from a pinned checkout, with its certificate delivered through LoadCredential
  license: UNLICENSED
  min_ansible_version: "2.16"
  platforms:
    - name: Ubuntu
      versions:
        - noble
  galaxy_tags:
    - nodejs
    - systemd

dependencies: []
