---
galaxy_info:
  role_name: hardening
  namespace: hcw
  author: HybridCloudWorks
  description: Key-only SSH, ufw, unattended upgrades and fail2ban for the lab host
  license: UNLICENSED
  min_ansible_version: "2.16"
  platforms:
    - name: Ubuntu
      versions:
        - noble
  galaxy_tags:
    - security
    - ssh
    - firewall

dependencies: []
