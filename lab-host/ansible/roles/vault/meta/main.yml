---
galaxy_info:
  role_name: vault
  namespace: hcw
  author: HybridCloudWorks
  description: HashiCorp Vault host-native from a signature-verified release, raft storage, TLS on the loopback only, never initialised by the role
  license: UNLICENSED
  min_ansible_version: "2.16"
  platforms:
    - name: Ubuntu
      versions:
        - resolute
        - noble
  galaxy_tags:
    - vault
    - secrets

dependencies: []
