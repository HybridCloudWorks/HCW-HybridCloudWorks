name: Deploy Functions

# ENABLED 2026-08-20, manual-dispatch only.
#
# The push trigger is deliberately commented out rather than deleted. Enabling
# this workflow and enabling auto-deploy-on-merge are two separate decisions,
# and only the first has been made: the Function App has never held code, so
# the first deploys are the ones most likely to need watching. Restore the push
# trigger once a dispatch run has succeeded end to end and the smoke test is
# trusted — not before, because a red auto-deploy on main is discovered by
# whoever next visits the site.
#
# on:
#   push:
#     branches: [main]
#     paths:
#       - 'functions/**'
on:
  workflow_dispatch:

# id-token: write is what lets the runner request a GitHub OIDC token. Without
# it azure/login has nothing to exchange and fails before reaching Azure.
permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    name: Deploy to Azure Functions
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: functions/package-lock.json

      - name: Install dependencies
        working-directory: functions
        run: npm ci

      # Federated OIDC — no stored credential. These three are repository
      # VARIABLES, not secrets: they identify the managed identity but grant
      # nothing without an OIDC token from this repo and ref. Their values come
      # from the `client_id` Terraform output and the subscription
      # and tenant the workspace targets.
      #
      # On AADSTS70021 ("no matching federated identity record found"), compare
      # the token's subject against the `federated_subjects`
      # output — the usual cause is a ref or repository name that has moved.
      - name: Azure Login
        uses: azure/login@f5d393ae46f8fde4be8b75f32e3fc50e654ad0ca # v3
        with:
          client-id: ${{ vars.CLIENT_ID }}
          tenant-id: ${{ vars.TENANT_ID }}
          subscription-id: ${{ vars.SUBSCRIPTION_ID }}

      # T-503: the Functions host storage account is default-Deny, and this
      # runner has a public dynamic IP, so the deployment-package upload needs
      # a firewall window: add this runner's IP, deploy, remove it in an
      # always-run cleanup. Authorized by the deploy identity's Storage
      # Account Contributor grant scoped to exactly this account (infra/
      # oidc.tf github_deploy_funcsa_network). The rule add is eventually
      # consistent — the short wait lets it propagate before the upload.
      - name: Open storage firewall window for this runner
        id: fw
        run: |
          set -euo pipefail
          RUNNER_IP=$(curl -sS --max-time 10 https://api.ipify.org)
          echo "runner_ip=${RUNNER_IP}" >> "$GITHUB_OUTPUT"
          az storage account network-rule add \
            --resource-group "${{ vars.RESOURCE_GROUP }}" \
            --account-name "${{ vars.FUNCTIONS_STORAGE_ACCOUNT }}" \
            --ip-address "${RUNNER_IP}" \
            --output none
          sleep 30

      # A repository VARIABLE, seeded from the `function_app_name` Terraform
      # output by scripts/set-github-variables.ps1 — never a literal here.
      #
      # It was a literal until 2026-08-20 and had gone stale twice over: this
      # step read `func-site-prod-scus` and functions/package.json read
      # `hcw-functions-prod`, an estate older still. Neither app exists, so the
      # first deploy would have failed on "app not found" — which reads as a
      # permissions or wrong-subscription problem long before it reads as a
      # name that was renamed out from under a hardcoded string.
      - name: Deploy to Azure Functions
        uses: Azure/functions-action@c5060b3b8bb1ebbcb531abd00c822ecbaa8ea656 # v1.5.7
        with:
          app-name: ${{ vars.FUNCTION_APP_NAME }}
          package: functions/
          respect-funcignore: true

      - name: Close storage firewall window
        if: ${{ always() && steps.fw.outputs.runner_ip != '' }}
        run: |
          az storage account network-rule remove \
            --resource-group "${{ vars.RESOURCE_GROUP }}" \
            --account-name "${{ vars.FUNCTIONS_STORAGE_ACCOUNT }}" \
            --ip-address "${{ steps.fw.outputs.runner_ip }}" \
            --output none

      - name: Post-deploy smoke test
        run: |
          echo "## 🚀 Azure Functions Deploy Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Through Cloudflare, NOT the azurewebsites.net origin.
          #
          # Two reasons, and the second is the one that bites. It tests the path
          # real traffic takes — origin lock, WAF, transform rules and all —
          # rather than a path only CI uses. And once
          # functions_origin_lock_enabled is true, the origin refuses everything
          # outside Cloudflare's ranges: a GitHub-hosted runner has a public
          # dynamic IP, so curling APP_HOSTNAME directly would fail on every
          # deploy, reported as a health-check failure rather than as the
          # firewall doing its job.
          #
          # FUNCTIONS_URL is the proxied api-azure.<domain> record and already
          # carries the /api suffix.
          # Capture the body as well as the code, and print BOTH to the step log.
          #
          # This used to report only into $GITHUB_STEP_SUMMARY, so a failure
          # produced a red step with an empty log and a status code visible
          # only on the summary tab. The three plausible causes here — the app
          # is down, the origin lock refused us, or Cloudflare served a bot
          # challenge — are indistinguishable without the body, and they need
          # completely different fixes.
          URL="${{ vars.FUNCTIONS_URL }}/health"
          echo "GET $URL"

          # x-hcw-ci-probe tells Cloudflare this runner is ours, so it is not
          # served the "Just a moment..." bot challenge. Without it this step
          # gets a 403 challenge page and reports a healthy app as broken.
          #
          # This is NOT the origin secret and must never be set to the same
          # value: that one is stamped by Cloudflare on the way to the origin
          # and is what the app trusts. This one travels the other way.
          BODY=$(curl -sS -m 30 -w $'\n%{http_code}' \
                   -H "x-hcw-ci-probe: ${{ secrets.CI_PROBE_SECRET }}" \
                   "$URL" 2>&1 || true)
          STATUS=$(printf '%s' "$BODY" | tail -n1)
          BODY=$(printf '%s' "$BODY" | sed '$d')

          echo "status: $STATUS"
          echo "body:   ${BODY:0:400}"

          {
            echo "## 🚀 Azure Functions Deploy Complete"
            echo ""
            echo "\`GET $URL\` → **HTTP $STATUS**"
          } >> $GITHUB_STEP_SUMMARY

          if [ "$STATUS" = "200" ]; then
            echo "✅ Health check: **PASSED**" >> $GITHUB_STEP_SUMMARY
          else
            {
              echo "❌ Health check: **FAILED**"
              echo ""
              echo "\`\`\`"
              echo "${BODY:0:800}"
              echo "\`\`\`"
              echo ""
              echo "A Cloudflare challenge page means the runner was bot-filtered"
              echo "(REVIEW.md §2.3) — not that the app is down."
            } >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
