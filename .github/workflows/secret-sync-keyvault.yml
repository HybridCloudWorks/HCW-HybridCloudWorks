name: DISABLED - Prototype Secret Sync

on:
  workflow_dispatch:

jobs:
  sync:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Sync Secrets
        run: |
          echo "Syncing secrets to Key Vault... (TODO: Implement actual mapping)"
          # Example:
          # az keyvault secret set --vault-name hcw-keyvault-prod --name "PUBLER-API-KEY" --value "${{ secrets.PUBLER_API_KEY }}"
